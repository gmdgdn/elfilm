This Astro app is deprecated for public ElFilm frontend work.

## Current Rule

- New public UX work belongs in [`../web/`](/E:/Eslam%20Builds/elfilm/web)
- `elfilm-web` is the active frontend on `https://film.gmd.gdn`
- `elfilm` is the backend/API worker
- `site/` is frozen and reference-only

## Working Here

Only touch this app when you are:

- reading it to port ElFilm-specific behavior into `web/`
- fixing a narrow issue required for reference or archival purposes

Do not add net-new public pages, deployment flows, or architecture docs that imply this app is still the primary site.
